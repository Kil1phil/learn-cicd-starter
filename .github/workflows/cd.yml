name: cd

on:
  push:
    branches: [main]

jobs:
  tests:
    name: Deploy
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.1"

      - name: Set up goose
        run : 'go install github.com/pressly/goose/v3/cmd/goose@latest'

      - name: Build
        run: ./scripts/buildprod.sh

      - name: Migrate
        run : ./scripts/migrateup.sh


      - id: 'auth'
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v3'

      - name: 'Use gcloud CLI'
        run: 'gcloud builds submit --tag europe-west10-docker.pkg.dev/notely-486711/notely-ar-repo/notely:v1 .'

      - name: Deploy to Cloud Run
        run: 'gcloud run deploy notely --image europe-west10-docker.pkg.dev/notely-486711/notely-ar-repo/notely:v1 --region europe-west10 --allow-unauthenticated --project notely-486711 --max-instances=4'

